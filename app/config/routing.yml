OATBundle:
    resource: "@OATBundle/Controller/"
    type:     annotation
    prefix:   /
    
logout:
    pattern:   /logout
    
login_check:
    pattern:   /login_check

# Internal routing configuration to handle ESI
#_internal:
#   resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#   prefix:   /_internal
